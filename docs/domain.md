# 领域约定

跨园托位协同服务围绕托位、候补申请、限时要约与跨园调度保存业务事实。主要对象为园区、班组、家庭、候补申请、要约、席位、整改限制、审计事件。

必须长期遵守以下约束：名额按园区、班组与服务日期核算；整改中的班组不得发出新要约；并发确认只能占用一个席位；过期要约释放后仍保留原因与操作者。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。